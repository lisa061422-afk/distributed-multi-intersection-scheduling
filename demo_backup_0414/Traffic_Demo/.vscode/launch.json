{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch Demo (Edge, no extension needed)",
            "url": "http://localhost:8080/warehouse_amr_demo_test.html",
            "webRoot": "${workspaceFolder}",
            "userDataDir": true,
            "preLaunchTask": "Start Python Server"
        }
    ]
}
